// SoC (Separation of Concerns): on va coder un dataMapper, donc un objet qui contient toutes les requêtes sql nécessaires à l'application

const Level = require("./models/level");
const client = require("./database");

const dataMapper = {
    
};

module.exports = dataMapper;